plugin.tx_csseo {

    # cat=plugin.tx_csseo/href; type=boolean; label= Enable hreflang-Tags
    hreflang.enable =

    # cat=plugin.tx_csseo/href; type=string; label= List the Language-Uids, which should be linked with a hreflang-Tag (e.g. 0,2,3)
    hreflang.ids =

    # cat=plugin.tx_csseo/href; type=string; label= List the Language-Keys for the hreflang-Tags - same order as hreflang.uids (e.g. en,de,ch)
    hreflang.keys =

    # cat=plugin.tx_csseo/social; type=string; label= Default image for open graph and twitter (e.g. fileadmin/user_upload/image.jpg)
    social.defaultImage =

    # cat=plugin.tx_csseo/social; type=string; label= Default twitter creator (without @)
    social.twitter.creator =

    # cat=plugin.tx_csseo/tracking; type=string; label= Google Analytics id (eg. UA-XXXXXX-XXX)
    tracking.googleAnalytics =

    # cat=plugin.tx_csseo/tracking; type=string; label= Enter a Piwik URL
    tracking.piwik =

    # cat=plugin.tx_csseo/tracking; type=string; label= Enter the ID for site
    tracking.piwik.id = 1
}

module.tx_csseo_mod1 {
    view {
        # cat=module.tx_csseo_mod1/file; type=string; label=Path to template root (BE)
        templateRootPath = EXT:cs_seo/Resources/Private/Templates/
        # cat=module.tx_csseo_mod1/file; type=string; label=Path to template partials (BE)
        partialRootPath = EXT:cs_seo/Resources/Private/Partials/
        # cat=module.tx_csseo_mod1/file; type=string; label=Path to template layouts (BE)
        layoutRootPath = EXT:cs_seo/Resources/Private/Layouts/
    }
    persistence {
        # cat=module.tx_csseo_mod1//a; type=string; label=Default storage PID
        storagePid =
    }
}